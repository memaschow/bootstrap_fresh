source 'https://rubygems.org'
ruby '1.9.3'

gem 'rails'
gem 'mail'
gem 'railties'
gem 'sorcery', :git => 'git://github.com/FlorianSuchan/sorcery.git', :branch => 'master'
gem 'fullcalendar-rails'
gem 'ice_cube'
gem 'rufus-scheduler'
gem 'faker'
gem 'twitter-bootstrap-rails'
gem 'bootstrap-sass', '2.1'
gem 'thor', '>=0.14.4'
gem 'jquery-ui-rails'
gem 'uglifier', '>= 1.0.3'
gem 'jquery-rails'
gem 'jquery-ui-themes'
gem 'binding_of_caller'

# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sass-rails'
  gem 'coffee-rails'
end

group :development do
  gem 'sqlite3'
  gem 'better_errors'
end

group :test do
  gem 'fabrication'
  gem 'faker'
end

group :production do
  gem 'pg'
end

# To use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.0.0'

# To use Jbuilder templates for JSON
# gem 'jbuilder'

# Use unicorn as the app server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger
gem 'debugger', group: [:development, :test]
